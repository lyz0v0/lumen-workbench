{
  "name": "lumen-workbench-app",
  "version": "0.13.5",
  "private": true,
  "description": "Lumen 本地 AI 工作台 —— 多文件源码工程，构建产物为压缩混淆后的 dist",
  "scripts": {
    "build": "node build.mjs",
    "dev": "node build.mjs --dev"
  },
  "devDependencies": {
    "esbuild": "^0.24.2"
  }
}
